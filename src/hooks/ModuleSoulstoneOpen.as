package hooks
{
	
	/**
	 * Open the soulstone module ui.
	 * 
	 * Callback parameters:
	 * - minimized:Boolean = false,	Load the interface in minimized mode ?
	 * 
	 * @author Relena
	 */
	public class ModuleSoulstoneOpen
	{
	}
}